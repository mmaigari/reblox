<%= form_with(model: contact_u) do |form| %>
  <% if contact_u.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contact_u.errors.count, "error") %> prohibited this contact_u from being saved:</h2>

      <ul>
        <% contact_u.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

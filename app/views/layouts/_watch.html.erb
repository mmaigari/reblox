<div>
  <a href="<%= base_about_path %>" class="relative text-white rounded-full px-4 py-2 hover:bg-opacity-80 gradient-outline">See how it works</a>
</div>








<style>
.gradient-outline {
  position: relative;
  z-index: 1;
}

.gradient-outline::before {
  content: '';
  position: absolute;
  top: -2px;
  left: -2px;
  right: -2px;
  bottom: -2px;
  background: linear-gradient(90deg, #0fc3ff, #00234c);
  z-index: -1;
  border-radius: 9999px; /* Full rounded corners */
}

.gradient-outline::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: #00234c; /* Button background color */
  z-index: -1;
  border-radius: 9999px; /* Full rounded corners */
}
</style>
